# ElevateIQ — Flask CLI Environment Configuration
# Loaded automatically by Flask CLI via python-dotenv
# Do NOT commit secrets here — use .env for those

FLASK_APP=backend.wsgi
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_RUN_HOST=0.0.0.0
FLASK_RUN_PORT=5000
